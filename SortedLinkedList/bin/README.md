1. Implementirajte metodu `add(int n)` tako da doda broj `n` u linkanu listu uzimajuæi u obzir da linkana lista nakon pokretanja treba biti sortirana u rastuæem redoslijedu
* ako vam se ovaj zadatak pokaže kao suviše komplikovan, a želite pokušati uraditi ostale zadatke, implementirajte `add(n)` metodu tako da dodaje element na kraj liste. Sliène primjere smo veæ radili na GitHub-u.

2. Dovršite implementaciju metode `toArray()` tako da vraæa niz koji sadrži sve elemente iz liste u ispravnom redoslijedu.

3. *(bonus)* Prepišite metodu `getLength()` tako da raèuna dužinu liste reukurzivno umjesto iterativno.